# Nginx reverse proxy for Biblia Sagrada API
# SSL: use certbot/Let's Encrypt and uncomment the server block with listen 443 ssl

events {
    worker_connections 1024;
}

http {
    upstream api {
        server api:8000;
    }

    # Redirect HTTP to HTTPS in production (uncomment when SSL is active)
    # server {
    #     listen 80;
    #     server_name api.seudominio.com;
    #     return 301 https://$server_name$request_uri;
    # }

    server {
        listen 80;
        server_name _;

        client_max_body_size 10M;

        location / {
            proxy_pass http://api;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_connect_timeout 60s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
        }

        location /health {
            proxy_pass http://api/health;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            access_log off;
        }
    }

    # Production HTTPS (uncomment and set paths after running certbot)
    # server {
    #     listen 443 ssl http2;
    #     server_name api.seudominio.com;
    #     ssl_certificate /etc/letsencrypt/live/api.seudominio.com/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/api.seudominio.com/privkey.pem;
    #     include /etc/letsencrypt/options-ssl-nginx.conf;
    #     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
    #
    #     client_max_body_size 10M;
    #     location / {
    #         proxy_pass http://api;
    #         proxy_http_version 1.1;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}
